{
  "schemaVersion": "1.0.0",
  "projectStateVersion": 3,
  "summary": "App deployment: fix startup hang on “Connecting to backend” by removing frontend call to missing backend method `_initializeAccessControlWithSecret` (actor creation was failing).",
  "architectural_notes": [
    "Frontend startup must not call backend/actor methods that are not implemented; actor initialization should fail fast with clear error or guard optional admin initialization."
  ],
  "known_issues": [
    "App stuck on “Connecting to backend” due to frontend calling missing backend method `_initializeAccessControlWithSecret`, preventing actor creation."
  ],
  "isFixRequest": true,
  "existing_features": [
    {
      "id": "feature-stop-frontend-startup-from-calling-the-missing-backend-method-initializeaccesscontrolwithsecret-so-authenticated-users-no-longer-get-stuck-on-the-connecting-to-backend-startup-phase",
      "title": "Stop frontend startup from calling the missing backend method `_initializeAccessControlWithSecret` so authenticated users no longer get stuck on the “Connecting to backend…” startup phase.",
      "reqIds": [
        "REQ-1"
      ],
      "version": 1
    },
    {
      "id": "feature-because-frontend-src-hooks-useactor-ts-is-immutable-implement-a-new-non-immutable-actor-hook-that-creates-the-backend-actor-anonymous-and-authenticated-without-calling-initializeaccesscontrolwithsecret-and-update-all-editable-frontend-modules-to-import-and-use-the-new-hook-instead-of-useactor",
      "title": "Because `frontend/src/hooks/useActor.ts` is immutable, implement a new non-immutable actor hook that creates the backend actor (anonymous and authenticated) without calling `_initializeAccessControlWithSecret`, and update all editable frontend modules to import and use the new hook instead of `useActor`.",
      "reqIds": [
        "REQ-2"
      ],
      "version": 1
    }
  ],
  "new_feature_requests": [],
  "projectName": "Fix startup hang on “Connecting to backend” by removing missing admin init call",
  "clarification_mode": "instant",
  "clarification_rounds": 0
}